name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x64
            artifact: zond
            archive: tar.gz
          - os: macos-latest
            target: darwin-arm64
            artifact: zond
            archive: tar.gz
          - os: windows-latest
            target: win-x64
            artifact: zond.exe
            archive: zip

    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.0

      - run: bun install

      - name: Build binary
        run: bun build --compile src/cli/index.ts --outfile zond

      - name: Package (tar.gz)
        if: matrix.archive == 'tar.gz'
        run: tar -czf zond-${{ matrix.target }}.tar.gz ${{ matrix.artifact }}

      - name: Package (zip)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: Compress-Archive -Path ${{ matrix.artifact }} -DestinationPath zond-${{ matrix.target }}.zip

      - uses: actions/upload-artifact@v5
        with:
          name: zond-${{ matrix.target }}
          path: zond-${{ matrix.target }}.${{ matrix.archive }}

  release:
    name: Publish Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v5
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            zond-linux-x64.tar.gz
            zond-darwin-arm64.tar.gz
            zond-win-x64.zip

  publish:
    name: Publish to npm
    needs: release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.0

      - run: bun install

      - run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
