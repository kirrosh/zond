{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write(*.yaml)",
        "hooks": [
          {
            "type": "command",
            "command": "zond validate \"$TOOL_INPUT_FILE_PATH\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
